{
  "name": "concurrent-memoize",
  "version": "1.0.0",
  "description": "Memoize Concurrent Requests in your Application so that only one request is executed and all subsequent, concurrent, waiting requests that are identical to the first request get notified with the result of the first call. Also all future accesses of the value return cache.",
  "main": "lib/memoize.js",
  "scripts": {
    "test": "node ./node_modules/.bin/mocha --require should --harmony --recursive ./test/memoize.test.js"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/sazze/node-concurrent-memoize.git"
  },
  "keywords": [
    "dedupe",
    "deduplicate",
    "request",
    "mysql"
  ],
  "author": "Adam Jaso <ajaso@sazze.com>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/sazze/node-deduplicate/issues"
  },
  "homepage": "https://github.com/sazze/node-deduplicate#readme",
  "dependencies": {
    "callback-and-promise": "^2.0.0",
    "co": "^4.6.0",
    "debug": "^2.2.0",
    "lodash": "^4.0.1"
  },
  "devDependencies": {
    "async": "^1.5.2",
    "mocha": "^2.3.4",
    "request": "^2.67.0",
    "should": "^8.2.0"
  }
}
